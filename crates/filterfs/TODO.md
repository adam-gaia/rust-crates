# TODO

- It would be cool to spawn a listener on a background thread that watches for a message sent to a socket (probalby unix socket) to disable/reenable filtering. Then small CLI client to send that message
- How to make it compatible with mount?
  - mount.filterfs?
